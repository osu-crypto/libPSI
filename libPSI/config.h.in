#pragma once
#include <cryptoTools/Common/config.h>
#include <libOTe/config.h>

#define ON 1


#if ENABLE_RELIC !=  @ENABLE_RELIC@
Config ERROR: ENABLE_RELIC flag does not match with libOTe
#endif

#if ENABLE_MIRACL !=  @ENABLE_MIRACL@
Config ERROR: ENABLE_MIRACL flag does not match with libOTe
#endif

#if ENABLE_SIMPLESTOT !=  @ENABLE_SIMPLESTOT@
Config ERROR: ENABLE_SIMPLESTOT flag does not match with libOTe
#endif

#if ENABLE_MR_KYBER !=  @ENABLE_MR_KYBER@
Config ERROR: ENABLE_MR_KYBER flag does not match with libOTe
#endif


// build the library with DCW PSI enabled
#cmakedefine ENABLE_DCW_PSI  @ENABLE_DCW_PSI@

// build the library with DKT PSI enabled
#cmakedefine ENABLE_DKT_PSI  @ENABLE_DKT_PSI@

// build the library with GRR PSI enabled
#cmakedefine ENABLE_GRR_PSI  @ENABLE_GRR_PSI@

// build the library with RR16 PSI enabled
#cmakedefine ENABLE_RR16_PSI  @ENABLE_RR16_PSI@

// build the library with RR17 PSI enabled
#cmakedefine ENABLE_RR17_PSI  @ENABLE_RR17_PSI@

// build the library with RR17 PSI enabled
#cmakedefine ENABLE_RR17B_PSI  @ENABLE_RR17B_PSI@

// build the library with KKRT PSI enabled
#cmakedefine ENABLE_KKRT_PSI  @ENABLE_KKRT_PSI@

// build the library with ECDH PSI enabled
#cmakedefine ENABLE_ECDH_PSI  @ENABLE_ECDH_PSI@

// build the library with DRRN PSI enabled
#cmakedefine ENABLE_DRRN_PSI  @ENABLE_DRRN_PSI@


#undef ON